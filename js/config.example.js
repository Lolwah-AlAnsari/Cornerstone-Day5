/* Copy this file to js/config.js and fill in your own project values.
   js/config.js is git-ignored. On Vercel it is generated at build time from
   the SUPABASE_URL and SUPABASE_ANON_KEY environment variables.

   The publishable ("anon") key is safe in the browser — it is designed for it.
   Row Level Security is what protects the data. Never put a service-role key here. */

window.SALFA_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT-REF.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-PUBLISHABLE-KEY",
};
